# Variant files for MeshCore

It is possible to use MeshCore on spare or unused LoRaPRS boards by simply copying or creating symlinks to directories  above into the MeshCore variants directory. After that, select one of configurations, build MeshCore and upload.
